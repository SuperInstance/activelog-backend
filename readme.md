# 🧪 Local Deployment Guide (Windows)

This guide walks you through running the DMlog.ai stack locally on Windows using PowerShell and Docker. It assumes you’ve cloned the repo and have Docker Desktop installed.

---

## ✅ Step 1: Configure Environment

1. Duplicate the example environment file: